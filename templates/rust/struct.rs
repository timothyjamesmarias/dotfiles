pub struct {{CLASS_NAME}} {
    // TODO: Add fields
}

impl {{CLASS_NAME}} {
    pub fn new() -> Self {
        Self {
            // TODO: Initialize fields
        }
    }
}

impl Default for {{CLASS_NAME}} {
    fn default() -> Self {
        Self::new()
    }
}
