{
  "title": "Test rule",
  "rules": [
    {
      "description": "TEST: right_shift = escape",
      "manipulators": [
        {
          "type": "basic",
          "from": { "key_code": "right_shift" },
          "to": [{ "key_code": "escape" }]
        }
      ]
    }
  ]
}
